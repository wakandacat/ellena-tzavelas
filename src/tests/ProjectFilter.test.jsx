import { describe, test, expect } from "vitest";
import ProjectFilter from "../components/ProjectFilter";
//test the ProjectFilter component

describe("ProjectFilter component", () => {
  //test that it fills the dropdown with more than just "All"
  //test that changing the filter updates the dropdown options
  //test that changing the filters only displays projects with that filter
  test.todo("that the ProjectFilter component functions correctly");
});
